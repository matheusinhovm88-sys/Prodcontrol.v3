[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "prodcontrol"
version = "3.0.0"
dependencies = [
    "Flask==3.0.2",
    "flask-cors==4.0.1",
    "firebase-admin==6.5.0",
    "gunicorn==21.2.0",
    "requests>=2.34.2"
]